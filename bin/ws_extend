#!/bin/bash

if [ $# -eq 2 ]
then
    ws_allocate -x $1 $2
else
    if [ $# -eq 4 ]
    then
    	ws_allocate -x $1 $2 $3 $4
    else
        echo "Usage: ws_extend [-F filesystem] workspace days"
    fi
fi
